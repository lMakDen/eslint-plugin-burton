const RULES = {
  NO_BAD_AWAIT: 'no-bad-await',
  FUNCTION_PARAMETERS_LIMIT: 'function-parameters-limit'
}

const TYPES = {
  suggestion: 'suggestion',
  problem: 'problem',
  layout: 'layout'
}

module.exports = {
  RULES,
  TYPES
}